# 感觉不是很有用，现在工作了不会天天在github或者gitlab上面创建仓库
# source ${0:A:h}/git/main.sh

# adb的alias。现在不用安卓机了也很少用了
# source ${0:A:h}/adb/main.sh

# linux和mac对rg的包名称不同，但不需要每次都单独安装
#source ${0:A:h}/rg/main.sh

# fzf的自定义配置，用得少，一般给edvim之类的用，而且随着fzf的迭代肯定会慢慢废弃掉
source ${0:A:h}/fzf/main.sh
